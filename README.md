# worldclockwatch-site

Static site for worldclockwatch.com, served by GitHub Pages from the root of `main`.
Put the site files (index.html and friends) directly in this folder, not in a subfolder. Keep `CNAME`.
